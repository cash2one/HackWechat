package com.tencent.mm.ui.tools;

import android.view.View;
import android.view.View.OnClickListener;

class HardCoderSettingUI$4 implements OnClickListener {
    final /* synthetic */ HardCoderSettingUI zkJ;

    HardCoderSettingUI$4(HardCoderSettingUI hardCoderSettingUI) {
        this.zkJ = hardCoderSettingUI;
    }

    public final void onClick(View view) {
        boolean isChecked = HardCoderSettingUI.a(this.zkJ).isChecked();
        HardCoderSettingUI.b(this.zkJ).setChecked(isChecked);
        HardCoderSettingUI.c(this.zkJ).setChecked(isChecked);
        HardCoderSettingUI.d(this.zkJ).setChecked(isChecked);
        HardCoderSettingUI.e(this.zkJ).setChecked(isChecked);
        HardCoderSettingUI.f(this.zkJ).setChecked(isChecked);
        HardCoderSettingUI.g(this.zkJ).setChecked(isChecked);
        HardCoderSettingUI.h(this.zkJ).setChecked(isChecked);
        HardCoderSettingUI.i(this.zkJ).setChecked(isChecked);
        HardCoderSettingUI.j(this.zkJ).setChecked(isChecked);
        HardCoderSettingUI.k(this.zkJ).setChecked(isChecked);
        HardCoderSettingUI.l(this.zkJ).setChecked(isChecked);
        HardCoderSettingUI.m(this.zkJ).setChecked(isChecked);
        HardCoderSettingUI.n(this.zkJ).setChecked(isChecked);
        HardCoderSettingUI.o(this.zkJ).setChecked(isChecked);
        HardCoderSettingUI.p(this.zkJ).setChecked(isChecked);
    }
}
