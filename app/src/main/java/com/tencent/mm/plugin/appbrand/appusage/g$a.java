package com.tencent.mm.plugin.appbrand.appusage;

enum g$a {
    FORCE_OFF(0),
    FORCE_ON(1),
    DYNAMIC_THRESHOLD(2);
    
    final int value;

    private g$a(int i) {
        this.value = i;
    }
}
