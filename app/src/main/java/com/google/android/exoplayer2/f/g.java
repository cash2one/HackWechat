package com.google.android.exoplayer2.f;

import com.google.android.exoplayer2.Format;

public interface g {
    public static final g awc = new 1();

    boolean g(Format format);

    e k(Format format);
}
