package com.d.a.a;

import java.io.UnsupportedEncodingException;

public final class h {
    static final int bhe = 5;
    static final int bhf = 7;
    static final byte[] bhg;
    static final int bhh = 4;
    static final int bhi = 6;
    static final byte[] bhj;
    static final byte[] bhk = new byte[]{(byte) -75, (byte) -5, (byte) -12, (byte) 112, (byte) -40, (byte) -44, (byte) -7, (byte) -71};
    static final String bhl = ("/" + y.PROTOCOL + "/c.php");
    static final String bhm = ("/" + y.PROTOCOL + "/d.php");
    static final String bhn = ("_" + y.bmF + "_list.sensewhere-sdk.com");

    static {
        byte[] bytes;
        try {
            bytes = "$UEC,".getBytes("UTF-8");
        } catch (UnsupportedEncodingException e) {
            bytes = "$UEC,".getBytes();
        }
        bhg = bytes;
        try {
            bytes = "$UP,".getBytes("UTF-8");
        } catch (UnsupportedEncodingException e2) {
            bytes = "$UP,".getBytes();
        }
        bhj = bytes;
    }
}
